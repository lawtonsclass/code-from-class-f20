// this is a single-line comment; it's only for the programmer. The
// compiler doesn't care about comments--they're not real code.

/*

In between these you can put
multi-line
comments

*/

// main is the function that is run first when the program starts
// it returns an int(eger)
int main() {
  // the "body" of main is between {}s
  // returning from main stops the program
  return 0;  // 0 is called an exit code
  // 0 stands for "success"; any other number means failure
}
