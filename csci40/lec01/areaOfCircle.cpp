#include <iostream>
using namespace std;  // now std::cout is just cout

int main() {
  // let's calculate the area of a circle with radius 2.25
  // ^ is *not* power
  cout << (3.14 * (2.25 * 2.25)) << endl;

  return 0;
}
