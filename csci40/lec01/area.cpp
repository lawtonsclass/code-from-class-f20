#include <iostream>   // for cout
using namespace std;  // so we don't have to say std::cout

int main() {
  // let's calculate the area of a circle
  // ^ is *not* the power operator in C++
  cout << (3.14 * 6.78 * 6.78) << endl;

  return 0;
}
